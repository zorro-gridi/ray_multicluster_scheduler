# 任务需求哦
严格自查系统集群资源快照更新机制是否符合约束条件

# 约束条件
- 1. 调度器启动时，更新一次系统资源快照，用于 policy 负载均衡策略进行目标集群决策
- 2. 当系统所有可用的集群资源使用率超过阈值，新任务进入指定待执行任务队列
- 3. 当待执行任务队列不为空时，每 5 秒轮询更新一次系统资源快照，决策是否有可用集群资源用于任务调度
- 4. 当所有待执行任务队列为空时，不需要轮询更新系统集群的资源快照

# 批判性分析
- 1. 如果以上条件约束与系统现有的调度机制相互矛盾，可进行否决，并说明理由